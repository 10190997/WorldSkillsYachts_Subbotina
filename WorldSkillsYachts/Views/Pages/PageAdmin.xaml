<Page
    x:Class="WorldSkillsYachts.Views.Pages.PageAdmin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WorldSkillsYachts.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="PageAdmin"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Button 
            x:Name="AdminButton"
            Grid.Column="0"
            Grid.Row="1"
            Style="{DynamicResource BigButton}"
            Content="Перейти в раздел для администратора" Click="AdminButton_Click"/>
        <Button 
            x:Name="ManagerButton"
            Grid.Column="1"
            Grid.Row="1"
            Height="auto"
            Width="auto"
            Style="{DynamicResource BigButton}"
            Content="Перейти в раздел для менеджера" Click="ManagerButton_Click"/>
    </Grid>
</Page>